<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Wickra.Shazam</RootNamespace>
    <AssemblyName>Wickra.Shazam</AssemblyName>

    <PackageId>Wickra.Shazam</PackageId>
    <Version>0.1.0</Version>
    <Authors>kingchenc</Authors>
    <Description>The data-driven history-fingerprint match core for .NET over the Wickra C ABI: build a Shazam from a spec, index an asset's history, match the current fingerprint against it.</Description>
    <PackageLicenseExpression>MIT OR Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/wickra-lib/wickra-shazam</PackageProjectUrl>
    <RepositoryUrl>https://github.com/wickra-lib/wickra-shazam</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>trading;fingerprint;similarity;pattern-matching;ffi;native</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <NoWarn>$(NoWarn);NU5128;CS1591</NoWarn>
  </PropertyGroup>

  <!--
    Supported native runtime identifiers. The release pipeline builds the C ABI
    per target triple and stages the libraries under
    WickraShazam/runtimes/<rid>/native/ before `dotnet pack`:
      win-x64   win-arm64   linux-x64   linux-arm64   osx-x64   osx-arm64
  -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <None Include="runtimes/**/native/*" Pack="true" PackagePath="runtimes/" Condition="Exists('runtimes')" />
  </ItemGroup>

</Project>
