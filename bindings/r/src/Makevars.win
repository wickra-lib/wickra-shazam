# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_shazam.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKSHZM_INC)
PKG_LIBS = -L$(WKSHZM_LIB) -l:wickra_shazam.dll
