[package]
name = "wickra-shazam"
description = "Reference command-line client over wickra-shazam-core: load a spec and an asset's history from CSV, index it, match the current fingerprint against the history, and print the matches."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

# docs.rs builds with every feature on, so the whole surface is documented.
[package.metadata.docs.rs]
all-features = true

[lints]
workspace = true

[[bin]]
name = "wickra-shazam"
path = "src/main.rs"

[dependencies]
# The data-driven fingerprint core (re-exports the Candle type).
wickra-shazam-core.workspace = true
serde_json.workspace = true
clap.workspace = true
